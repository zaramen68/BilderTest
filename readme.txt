Задача по-моему была переведена. Лучше было бы прочитать условия в оригинале (если в английском).

Часть задания вызвало вопросы. 

Первым делом, на ум пришло, что коль речь идет о "сборке проекта", то речь идет о сборщике пакетов, типа ament.
Что имелось ввиду под "сборкой проекта" здесь? Просто последовательное и параллельное выполнение неких функций?
Тогда это не сборка проекта, а его выполнение.
Что имесь ввиду под "скриптом сборки Вашего проекта" в решении?
Сделал, как понял.

Условие использовать только стандартную библиотеку наложило ограничение на использования json, yaml, xml. 
А как без них вводить дерево обьектов в проект? Пришлось использовать map. Получилось коряво. Словарей-то в си нет, не питон.

Зачем в классе Builder(size_t num_threads) число потоков? Из общих соображений мы его не должны знать. Кроме того, их число может меняться 
в процессе "сборки". Так в схеме 1->2 1->3 2->4 3->4 начинаем с одного потока, потом можем делать два, потом снова один.

Короче, часть условий кажется не полным, часть приводит к мысли об очень частных решениях более общей задачи.



